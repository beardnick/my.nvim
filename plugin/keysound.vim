" pip3 install pysdl2-dll 
" pip3 install pysdl2 
" 启动 Vim 时自动启动
let g:keysound_enable = g:mynvim_keysound_enable

" 设置默认音效主题，可以选择：default, typewriter, mario, bubble, sword
let g:keysound_theme = 'typewriter'

" 设置音量：0-1000
let g:keysound_volume = 1000
